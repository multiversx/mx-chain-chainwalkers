{
  "hash": {
    "type": "string",
    "description": "The hash of this miniblock encoded as hex.",
    "required": true
  },
  "senderShardID": {
    "type": "uint32",
    "description": "Uint32 representation of the sender shard id",
    "required": true
  },
  "receiverShardID": {
    "type": "uint32",
    "description": "Uint32 representation of the receiver shard id",
    "required": true
  },
  "miniblockType": {
    "type": "uint32",
    "description": "A miniblock can be of several types",
    "example": "TxBlock(0), StateBlock(30), PeerBlock(60), SmartContractResultBlock(90), InvalidBlock(120), ReceiptBlock(150), RewardsBlock(255)",
    "required": true
  },
  "transactions": {
    "type": "array<transaction.json>",
    "description": "",
    "required": true
  }
}